#!/bin/sh

export HOME=<%= @options[:home] %>

cd <%= @options[:install_dir] %>

exec 2>&1
exec \
    /opt/rbenv/shims/bundle exec foreman start
  #chpst -u <%= @options[:user] %> \
    #<%= @options[:foreman_path] %> start
